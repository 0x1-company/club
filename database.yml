users:
  id: string
  provider: string # apple.com or google.com
  packageInfo:
    latestOS: string
    appName: string
    appVersion: string
    buildNumber: string
  privates:
    email: string

domains:
  label: string
  address: string

transactions:
  createdAt: number